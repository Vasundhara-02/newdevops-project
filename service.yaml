apiVersion: v1
kind: Service
metadata:
  name: self-healing-service
spec:
  type: NodePort
  selector:
    app: self-healing
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007